# Cross-platform
numpy>=1.26
pynput>=1.7.6

# Recorder — Windows
dxcam>=0.0.5; platform_system == "Windows"
pywin32>=306; platform_system == "Windows"

# Recorder — macOS
mss>=9.0; platform_system == "Darwin"
pyobjc-framework-Quartz>=10.0; platform_system == "Darwin"
pyobjc-framework-Cocoa>=10.0; platform_system == "Darwin"

# Training stage
torch>=2.2
torchvision>=0.17
timm>=1.0

# RL stage (optional, only needed for Stage 3)
gymnasium>=0.29
stable-baselines3>=2.3

# Live rollout input emulation
pydirectinput>=1.0.4; platform_system == "Windows"
pyautogui>=0.9.54; platform_system == "Darwin"

# Logging
tensorboard>=2.16
